# Cloudflare Workers — static assets from `make release-static` (dist/).
# Change `name` before first deploy (must be unique in your account).
name = "dice-playground"
compatibility_date = "2025-06-07"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
html_handling = "auto-trailing-slash"
