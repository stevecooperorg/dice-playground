# Decision log — brief-dice-playground-2026-06-12

## 2026-06-12 — Create (inferred from repository)

- **Intent:** Create product brief without interactive discovery; source material is the shipped product and docs.
- **Inputs used:** `README.md`, `docs/README.md`, `docs/AGENT.md`, `docs/cookbook/README.md`, `.agents/skills/dice-playground-standards/SKILL.md`, `Cargo.toml`.
- **Not available:** `project-context.md` (no file in repo); user interview; market sizing; business metrics.
- **Stakes inferred:** Open-source passion/education product with a live public deployment; not positioned as a VC-backed SaaS in repo materials.
- **Assumptions tagged in brief** where product/market claims could not be verified from code alone.

## 2026-06-12 — Update (Steve: notebook north star)

- **Change signal:** Current playground is IDE-like (files, editor, separate terminal-style output). **End goal:** Jupyter / RStudio-style **literate programming**—`.dice` as step-by-step narrative; output as **formatted report** (text, charts, images), not a detached panel.
- **Brief impact:** Executive summary, Solution, Differentiation, Success criteria, Scope, and Vision rewritten to distinguish **current stepping stone** vs **north star**. Prior brief language that centered the IDE + output tabs as the product endpoint superseded for UX (engine/language/docs unchanged).
- **Captured in addendum:** Current `app.rs` layout; open format/execution/export questions for PRD.
- **Not decided:** Literate file format, per-cell eval API, collaboration, export pipeline.

## 2026-06-12 — Update (Steve: single file, one-shot eval)

- **Decisions:** (1) **All content in one `.dice` file**—no sidecar markdown or JSON notebook. (2) **Execution is whole-document only**—no run-cell / step-through; speed is milliseconds so one Run is sufficient.
- **Brief impact:** North star reframed as knit-once report (Quarto-like execution, notebook-like layout). Scope **Out** lists per-cell execution and split file formats. Removed incremental/chunk eval from **In**.
- **Addendum:** Decision table; Jupyter noted as layout-only reference; per-cell engine API marked not required.
- **Still open:** Concrete literate syntax in `.dice`, export pipeline, images, multi-file workspace longevity.

## 2026-06-12 — Update (Steve: tutorial/cookbook = literate .dice)

- **Decision:** If markdown-first literate `.dice` is the model, **tutorial and cookbook content are themselves literate `.dice` files**, executable as-is (`dice eval`, playground Run). Docs site is woven output of that corpus—not separate markdown with “load script” handoff.
- **Brief/research impact:** Success criteria, scope, vision, and research integration section updated. Replaces implied “long-term maybe” convergence and duplicate `docs/*.md` + `examples/*.dice` pattern as technical debt to migrate away from.
- **Architecture implication:** `build-tutorial-site.sh` / static deploy must call **weave** (and CI **eval**) on the same `.dice` sources.
